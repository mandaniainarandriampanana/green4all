import { Account } from "@/components/Account/Account";
import { MenuList } from "@/components/MenuList/MenuList";

export const Menu = () => {
  return (
    <div>
      <Account />
      <MenuList />
    </div>
  );
};
